#docRepo
  %h1 Document Repository
  

- if flash[:notice] and flash[:notice]!="Signed in successfully."
  #flashNotice
    = flash[:notice]

- @categories.each do |category|

  %h2#committeeTitle= "#{category.name}"

  #document_content
    %table
      %thead
        %tr
          %td File name
          %td Updated at
      %tbody
        - documents = category.documents
        - if documents.empty?
          %td= "No files have been entered"
        - documents.each do |file|
          %tr
            %td= link_to file.title, info_file_path(file.id)
            %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")
        
= link_to 'Add new file', new_file_path, :class => "button"


/ #docRepo
/   %h1 Document Repository
  

/ - if flash[:notice] and flash[:notice]!="Signed in successfully."
/   #flashNotice
/     = flash[:notice]

/ %h2#committeeTitle About Community Grows

/ #document_content
/   %table
/     %thead
/       %tr
/         %td File name
/         %td Updated at
/     %tbody
/       - documents = Document.where(:committee_type => 'CGadmin')
/       - if documents.empty?
/         %td= "No files have been entered"
/       - documents.each do |file|
/         %tr
/           %td= link_to file.title, info_file_path(file.id)
/           %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")
        
/ %h2#committeeTitle Board Overview

/ #document_content
/   %table
/     %thead
/       %tr
/         %td File name
/         %td Updated at
/     %tbody
/       - documents = Document.where(:committee_type => 'boardoverview')
      
/       - if documents.empty?
/         %td= "No files have been entered"
/       - documents.each do |file|
/         %tr
/           %td= link_to file.title, info_file_path(file.id)
/           %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")
        
/ %h2#committeeTitle Board Activities

/ #document_content
/   %table
/     %thead
/       %tr
/         %td File name
/         %td Updated at
/     %tbody
/       - documents = Document.where(:committee_type => 'boardactivities')
      
/       - if documents.empty?
/         %td= "No files have been entered"
/       - documents.each do |file|
/         %tr
/           %td= link_to file.title, info_file_path(file.id)
/           %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")

/ %h2#committeeTitle Budgets and Finances

/ #document_content
/   %table
/     %thead
/       %tr
/         %td File name
/         %td Updated at
/     %tbody
/       - documents = Document.where(:committee_type => 'budgetfinance')
/       - if documents.empty?
/         %td= "No files have been entered"
/       - documents.each do |file|
/         %tr
/           %td= link_to file.title, info_file_path(file.id)
/           %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")

/ %h2#committeeTitle AB Meetings

/ #document_content
/   %table
/     %thead
/       %tr
/         %td File name
/         %td Updated at
/     %tbody
/       - documents = Document.where(:committee_type => 'abmeetings')
      
/       - if documents.empty?
/         %td= "No files have been entered"
/       - documents.each do |file|
/         %tr
/           %td= link_to file.title, info_file_path(file.id)
/           %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")

/ %h2#committeeTitle Board Resources

/ #document_content
/   %table
/     %thead
/       %tr
/         %td File name
/         %td Updated at
/     %tbody
/       - documents = Document.where(:committee_type => 'boardresources')
      
/       - if documents.empty?
/         %td= "No files have been entered"
/       - documents.each do |file|
/         %tr
/           %td= link_to file.title, info_file_path(file.id)
/           %td= file.updated_at.strftime("%m/%d/%Y %l:%M %p")
          

/ = link_to 'Add new file', new_file_path, :class => "button"
